[GoogleSheetConfig]
sheet_name = feed  # The name of the sheet tab where data is fetched
image_sizes_result_file = ImageSize.xlsx  # The name of the new Excel file to be created
size_column_name = SIZE  # The name of the column to update with image sizes
